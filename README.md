# Pro Calculator (Flutter)

Professional Flutter calculator app with offline history, scientific operations, and Arabic UI.

The Flutter project lives in `flutter_app/`. See `flutter_app/README.md` for build details.

The APK is built automatically by GitHub Actions on every push.
Find the latest APK in the **Releases** tab or **Actions** artifacts.
